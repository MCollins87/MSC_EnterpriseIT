apiVersion: v1
kind: Service
metadata:
  name: image-recognition-service
spec:
  type: LoadBalancer
  selector:
    app: image-recognition
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000